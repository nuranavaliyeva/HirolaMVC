@using HirolaMVC.Models

@model List<Product>

<div class="col-lg-12">
    <div class="swiper-container product-slider">
        <div class="swiper-wrapper">
            @foreach (Product product in Model)
            {

                <div class="swiper-slide product-item">
                    <div class="product-img">
                        <a asp-controller="shop" asp-action="detail" asp-route-id="@product.Id">
                            <img class="primary-img"
                                 src="~/assets/images/@product.ProductImages.FirstOrDefault(pi=>pi.IsPrimary==true)?.Image"
                                 alt="Product Images" />
                            <img class="secondary-img"
                                 src="~/assets/images/@product.ProductImages.FirstOrDefault(pi=>pi.IsPrimary==false)?.Image"
                                 alt="Product Images" />
                        </a>
                        <div class="product-add-action">
                            <ul>
                                
                               
                                <li>
                                    <a class="cus-add-basket" asp-action="AddBasket" asp-controller="Basket" asp-route-id="@product.Id"
                                       data-tippy="Add to cart"
                                       data-tippy-inertia="true"
                                       data-tippy-animation="shift-away"
                                       data-tippy-delay="50"
                                       data-tippy-arrow="true"
                                       data-tippy-theme="sharpborder">
                                        <i class="pe-7s-cart"></i>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                    <div class="product-content">
                        <a class="product-name"
                           asp-controller="shop" asp-action="detail" asp-route-id="@product.Id">@product.Name</a>
                        <div class="price-box pb-1">
                            <span class="new-price">$@product.Price</span>
                        </div>
                   
                    </div>
                </div>
            }

        </div>
    </div>
</div>